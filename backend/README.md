# Backend

The MVP is starting with a frontend-first architecture backed by Supabase services.

This folder is reserved for future server-side code if we later add:

- API composition or orchestration endpoints
- scheduled analytics jobs
- third-party integrations that should not run in the browser

No standalone backend service is required for the current app.
